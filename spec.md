# 快递查询微信小程序 — 产品规格文档 (spec.md)

> V1 版本 | 2026-04-25

---

## 1. 项目概述

### 1.1 产品定位
一款快递物流查询微信小程序，用户输入快递单号即可查询实时物流轨迹。

### 1.2 核心价值
- **用户侧**：一键查快递，无需下载 App，无需注册
- **商业侧**：通过微信小程序广告（Banner + 激励视频）变现
- **增长引擎**：SEO/GEO 搜索流量，覆盖快递查询长尾关键词（注：搜索收录和排序由微信平台决定，非确定性结果，V1 仅做基础配置，不作为硬性交付指标）

### 1.3 技术栈
| 层 | 方案 |
|---|---|
| 前端 | 微信小程序（Skyline 渲染引擎） |
| 后端 | 微信云开发（云函数 + 云数据库） |
| 数据源 | 快递100实时查询 API |
| 广告 | 微信小程序广告组件 |

---

## 2. UI 设计规范

### 2.1 风格
微信原生风，使用 WeUI 组件规范，零学习成本。

### 2.2 配色（对标顺丰品牌色）
| 用途 | 色值 | 说明 |
|---|---|---|
| 主色 | `#111111` | 导航栏、按钮文字、正文标题 |
| 强调色 | `#E60012` | 查询按钮背景、状态高亮、链接 |
| 页面背景 | `#F5F5F5` | 浅灰底色 |
| 卡片背景 | `#FFFFFF` | 内容卡片 |
| 辅助文字 | `#999999` | 次要信息、时间戳 |
| 分割线 | `#E5E5E5` | 列表分割 |

### 2.3 排版
- 字体：微信原生字体栈（不引入外部字体）
- 正文：28rpx（14px）
- 标题：32rpx（16px），bold
- 大标题：36rpx（18px），bold
- 间距：页面左右 padding 32rpx

### 2.4 组件规范
- 按钮：圆角 8rpx，查询按钮使用强调色背景 + 白色文字
- 输入框：白底，1rpx `#E5E5E5` 边框，圆角 8rpx，高度 80rpx
- 卡片：白底，圆角 16rpx，无阴影（或极轻阴影）
- 列表项：高度 100rpx，底部分割线
- 不使用自定义图标库，使用微信原生 icon 或纯文字

---

## 3. 页面结构

### 3.1 首页（查询页）`pages/index/index`

**页面元素（从上到下）：**

1. **导航栏**：标题"快递查询"
2. **输入区域**（白色卡片）：
   - 快递单号输入框（placeholder: "请输入快递单号"）
   - 快递公司选择器（默认"自动识别"，可选手动选择具体公司；选了"顺丰"时显示手机号输入框）
   - 顺丰手机号输入框（仅手动选择"顺丰速运"时显示，placeholder: "顺丰需填写手机号后4位"）
   - 查询按钮（红色背景"查询"）
   - 剩余查询次数提示（灰色小字"今日还可查询 X 次"）
3. **查询历史区域**：
   - 标题"查询历史" + 清空按钮
   - 历史列表：每条显示单号 + 快递公司名 + 最新状态 + 查询时间
   - 点击可快速重新查询
   - 无历史时显示空状态提示
4. **Banner 广告位**

**页面标题**：快递查询 - 物流查询助手

**交互逻辑：**
- 输入单号后点击查询 → 调用云函数 → 跳转结果页
- 按钮防抖：点击后 loading 3秒，期间不可重复点击
- 单号格式校验：6-30位字母数字
- 快递公司选择器逻辑：默认"自动识别"（com=auto），用户可手动切换为具体公司。手动选择"顺丰速运"时，手机号后4位输入框出现并标记为必填
- 次数用完时：弹窗提示"今日查询次数已用完"，选项：看广告获得额外5次 / 关闭

**顺丰手机号处理策略：**
- com=auto（默认）：后端查询时若快递100返回需要手机号的错误，前端提示用户"该单号需要手机号后4位，请手动选择顺丰速运并填写"，用户补填后重新查询
- 手动选择顺丰：前端直接显示手机号输入框，必填校验通过后才发起查询

---

### 3.2 结果页 `pages/result/result`

**页面元素（从上到下）：**

1. **导航栏**：标题"物流详情"，返回按钮
2. **快递信息卡片**：
   - 快递公司名称
   - 快递单号（可复制）
   - 当前状态标签（已签收/派送中/在途/有问题）
3. **物流轨迹时间轴**：
   - 最新一条高亮显示（红色）
   - 其余灰色
   - 每条显示：时间 + 物流描述
   - 左侧竖线 + 圆点连接
4. **操作区**：
   - 分享按钮
5. **Banner 广告位**

**页面标题**：XX快递单号XXXX - 物流详情

**交互逻辑：**
- 从首页携带参数（queryResult JSON）进入
- 快递单号点击复制到剪贴板
- 分享卡片携带单号参数；好友点击后跳转首页并预填单号，好友需自行点击查询（不会消耗分享者的次数）。分享定位为"分享查询入口"，而非"分享查询结果"

---

### 3.3 快递公司专题页（SEO）`pages/company/[name]`

**覆盖的快递公司（共8个）：**

| 页面路径 | 标题关键词 | 快递100代码 |
|---|---|---|
| `pages/company/shunfeng` | 顺丰快递查询 | shunfeng |
| `pages/company/zhongtong` | 中通快递查询 | zhongtong |
| `pages/company/yuantong` | 圆通快递查询 | yuantong |
| `pages/company/yunda` | 韵达快递查询 | yunda |
| `pages/company/shentong` | 申通快递查询 | shentong |
| `pages/company/jitu` | 极兔速递查询 | jtexpress |
| `pages/company/jingdong` | 京东物流查询 | jd |
| `pages/company/ems` | EMS快递查询 | ems |

**页面元素（从上到下）：**

1. **导航栏**：标题"[公司名]快递查询"
2. **公司信息区**：
   - 公司名称
   - 简介（2-3句）
   - 单号格式说明
3. **查询入口**：
   - 输入框 + 查询按钮（跳转首页并预填快递公司参数）
4. **常见问题**（SEO 内容）：
   - 如何查询[公司名]快递？
   - [公司名]快递单号格式是什么？
   - [公司名]快递客服电话是多少？
5. **其他快递查询**（内链）：
   - 链接到其他7个专题页

**页面标题**：[公司名]快递查询 - 快递查询助手

**SEO 目的：**
- 覆盖"XX快递查询"长尾搜索词
- 微信搜索收录独立页面路径（收录时机和排序由平台决定，非确定性）
- 内链提升页面权重

---

## 4. 后端设计

### 4.1 云函数

#### `query` — 快递查询

**请求参数：**
```json
{
  "num": "SF1234567890",
  "com": "auto",
  "phone": "1234"  // 可选，顺丰必填
}
```

**处理流程：**
```
1. 获取调用者 openid（云函数自动注入）
2. 参数校验（单号格式、必填项）
3. 查询云数据库 rate_limit，检查今日查询次数
   - count >= 10 + bonus → 返回 { code: "LIMIT_EXCEEDED" }
4. 检查单号级缓存 query_cache
   - 缓存存在且未过期（30分钟内）→ 直接返回缓存结果，扣 count + 1
   - 缓存不存在或已过期 → 继续步骤 5
5. 构造快递100请求：
   - param = JSON.stringify({ com, num, phone })
   - sign = MD5(param + key + customer).toUpperCase()（注意：快递100要求32位大写十六进制）
   - POST poll.kuaidi100.com/poll/query.do
6. 更新 rate_limit：count + 1, lastQueryTime = now
7. 写入 query_cache（以单号为 key，TTL 30分钟）
8. 返回查询结果
```

**返回格式（成功）：**
```json
{
  "code": "OK",
  "data": {
    "nu": "SF1234567890",
    "com": "shunfeng",
    "state": "3",
    "status": "200",
    "traces": [
      { "time": "2026-04-25 14:30:00", "context": "快件已签收" }
    ]
  }
}
```

**返回格式（错误码完整列表）：**
```json
{ "code": "LIMIT_EXCEEDED", "message": "今日查询次数已用完" }
{ "code": "INVALID_PARAM", "message": "快递单号格式不正确" }
{ "code": "PHONE_REQUIRED", "message": "该快递公司需要手机号后4位验证" }
{ "code": "QUERY_ERROR", "message": "快递100返回的具体错误信息" }
```

#### `reward` — 广告奖励

**防刷机制：**
- 每个用户每日最多领取3次奖励（即每日上限 10 + 15 = 25次）
- 前端生成随机 nonce（UUID），云函数校验 nonce 是否已使用（防重放）
- V1 采用客户端验证 + 服务端防重放；V2 接入微信广告服务端回调做完整验证

**请求参数：**
```json
{
  "nonce": "uuid-generated-by-client"
}
```

**处理流程：**
```
1. 获取 openid
2. 校验 nonce：查询 reward_nonces 集合，若已存在则拒绝（防重放）
3. 查询 rate_limit：
   - 若 rewardCount >= 3 → 返回 { code: "REWARD_LIMIT" }
4. bonus += 5, rewardCount += 1
5. 记录 nonce 到 reward_nonces 集合
6. 更新 rate_limit
7. 返回新的可用次数
```

**返回格式：**
```json
{ "code": "OK", "remaining": 5 }
{ "code": "REWARD_LIMIT", "message": "今日广告奖励次数已用完" }
{ "code": "DUPLICATE_NONCE", "message": "无效请求" }
```

### 4.2 云数据库

#### `rate_limit` 集合

| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | 自动生成 |
| `_openid` | string | 用户 openid（云函数自动写入） |
| `date` | string | 当天日期 "YYYY-MM-DD" |
| `count` | number | 今日已查询次数 |
| `bonus` | number | 广告奖励额外次数 |
| `rewardCount` | number | 今日已领取广告奖励次数（上限3） |
| `lastQueryTime` | number | 上次查询时间戳（ms） |

**规则：**
- 每日可用次数 = 10 + bonus（bonus 上限 15，即 rewardCount 上限 3）
- 每日首次查询时创建记录（date = 当天）
- 查询时先检查 date 是否为当天，非当天则重置 count=0, bonus=0, rewardCount=0, date=当天
- 无需定时清理，历史记录不影响逻辑

**数据权限：** 仅管理员可读写（云函数有权操作，前端不可直接操作）

#### `query_cache` 集合（单号级缓存）

| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | 快递单号（作为唯一 key） |
| `result` | object | 快递100返回的完整结果 |
| `com` | string | 快递公司代码 |
| `state` | string | 物流状态 |
| `createdAt` | number | 缓存写入时间戳（ms） |

**规则：**
- 同一单号30分钟内的重复查询直接返回缓存，不调用快递100 API
- 避免触发快递100"同一单号查询频率至少半小时"的限制
- 不同用户查同一单号也走缓存（节省 API 调用）
- 缓存过期后下次查询重新调用 API 并更新缓存

**数据权限：** 仅管理员可读写

#### `reward_nonces` 集合（防重放）

| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | nonce（UUID，客户端生成） |
| `_openid` | string | 使用该 nonce 的用户 |
| `createdAt` | number | 使用时间戳 |

**规则：**
- 每个 nonce 只能使用一次
- 每日重置（按 createdAt 清理前一天记录，或忽略旧记录）
- 前端每次点"看广告"时生成新 UUID 作为 nonce

---

## 5. 前端数据层

### 5.1 本地存储（查询历史）

**存储 key：** `query_history`

**数据结构：**
```json
[
  {
    "num": "SF1234567890",
    "com": "shunfeng",
    "comName": "顺丰速运",
    "state": "3",
    "stateName": "已签收",
    "lastTrace": "快件已签收，签收人：本人",
    "queryTime": 1714036800000
  }
]
```

**规则：**
- 最多保留 20 条
- 最新查询排在最前
- 相同单号再次查询时更新而非重复添加
- 用户可手动清空

### 5.2 全局数据

**app.js globalData：**
```json
{
  "openid": "",
  "dailyLimit": 10,
  "dailyRemaining": 10
}
```

---

## 6. 广告方案

> **V1 推迟**：流量主资质暂未满足（需累计独立访客 >= 1000），广告功能推迟到 V2。V1 仅做纯查询工具，次数用完后直接提示"明天再来"。

### 6.1 广告位配置（V2 实现）

| 位置 | 类型 | 组件 | 展示条件 |
|---|---|---|---|
| 首页底部 | Banner | `<ad unit-id="xxx">` | 始终展示 |
| 结果页底部 | Banner | `<ad unit-id="xxx">` | 始终展示 |
| 次数用完时 | 激励视频 | `<rewarded-video-ad>` | 用户主动触发 |

### 6.2 激励视频流程（V2 实现）
```
用户点击查询 → 提示次数用完 → 弹窗：
  "今日查询次数已用完，观看广告可获得额外5次查询"
  [看广告获取] [以后再说]
                                    ↓ 点击看广告
                          调用 rewardedVideoAd.show()
                                    ↓ 观看完成回调
                          调用 reward 云函数
                                    ↓
                          提示"已获得5次额外查询"
                          自动执行刚才的查询
```

### 6.3 流量主资质
- 微信小程序开通流量主需要：累计独立访客 ≥ 1000
- **初期策略**：广告位代码预留，通过 `isFlowOwner` 变量控制是否展示
- 达到1000访客后开启广告展示

---

## 7. 风控策略

### 7.1 频次限制
| 维度 | 限制 | 实现位置 |
|---|---|---|
| openid/天 | 10次 + 广告奖励（每日最多领3次，上限25次） | 云函数 → 云数据库 rate_limit |
| 单号级缓存 | 同一单号30分钟内走缓存，不调快递100 API | 云函数 → 云数据库 query_cache |
| 广告奖励防刷 | nonce 防重放 + 每日最多领3次 | 云函数 → 云数据库 reward_nonces + rate_limit |
| 前端防抖 | 3秒 | 前端按钮 loading |

### 7.2 参数校验
- 快递单号：6-30位，允许字母、数字、连字符（-）
- 手机号后4位：4位纯数字（顺丰时必填）

### 7.3 安全措施
- 快递100 API Key 仅存在于云函数环境变量中，前端不可见
- 云数据库设置权限：仅管理员可读写
- 所有查询通过云函数中转，前端不直接调用快递100

---

## 8. SEO/GEO 优化

### 8.1 小程序信息
- **名称**：快递查询 - 物流查询助手
- **简介**：支持顺丰、中通、圆通、韵达、申通、极兔、京东、EMS等快递物流查询
- **服务类目**：快递物流

### 8.2 页面 SEO
- sitemap.json：所有页面允许索引
- 每个页面通过 `<page-meta>` 设置独立的 title
- 快递公司专题页路径含关键词拼音（如 `pages/company/shunfeng`）
- 专题页之间内链互链，提升页面权重

### 8.3 内容 SEO
- 每个专题页包含丰富的快递公司信息文本内容
- 包含常见问题（FAQ）结构化内容
- 页面内容与搜索关键词高度相关

---

## 9. 文件结构

```
kuaidi/
├── app.js                          # 云开发初始化 + 全局数据
├── app.json                        # 页面注册 + 云开发配置
├── app.wxss                        # 全局样式（顺丰配色变量）
├── project.config.json             # 项目配置
├── sitemap.json                    # SEO 索引配置
│
├── cloudfunctions/
│   ├── query/
│   │   ├── index.js                # 查询云函数
│   │   ├── config.json             # 环境变量（API凭证）
│   │   └── package.json
│   └── reward/
│       ├── index.js                # 广告奖励云函数
│       └── package.json
│
├── pages/
│   ├── index/
│   │   ├── index.js                # 首页逻辑
│   │   ├── index.json
│   │   ├── index.wxml              # 首页模板
│   │   └── index.wxss              # 首页样式
│   ├── result/
│   │   ├── result.js               # 结果页逻辑
│   │   ├── result.json
│   │   ├── result.wxml             # 结果页模板（时间轴）
│   │   └── result.wxss             # 结果页样式
│   └── company/
│       ├── shunfeng.js/json/wxml/wxss
│       ├── zhongtong.js/json/wxml/wxss
│       ├── yuantong.js/json/wxml/wxss
│       ├── yunda.js/json/wxml/wxss
│       ├── shentong.js/json/wxml/wxss
│       ├── jitu.js/json/wxml/wxss
│       ├── jingdong.js/json/wxml/wxss
│       └── ems.js/json/wxml/wxss
│
├── components/
│   └── navigation-bar/             # 已有，保持不变
│
├── utils/
│   ├── api.js                      # 云函数调用封装
│   ├── storage.js                  # 本地存储（查询历史）
│   └── constants.js                # 快递公司代码映射
│
└── spec.md                         # 本文档
```

---

## 10. 实施计划

### Step 1：云开发环境 + 云函数
1. app.json 开启云开发，app.js 初始化
2. 编写 `query` 云函数（API 代理 + 风控 + MD5签名）
3. 编写 `reward` 云函数（广告奖励）
4. 创建云数据库 `rate_limit` 集合
→ **验证**：开发者工具中调用云函数，能正确返回快递100查询结果

### Step 2：首页
1. 重写 index 页面（输入框 + 查询按钮 + 历史列表）
2. 接入云函数调用 + 本地历史存储
3. 全局样式（顺丰配色）
→ **验证**：输入真实快递单号，查询成功并展示

### Step 3：结果页
1. 创建 result 页面（物流时间轴）
2. 分享卡片配置
→ **验证**：查询后跳转结果页，物流轨迹正确展示

### Step 4：快递公司专题页
1. 创建 constants.js（快递公司代码映射）
2. 为8个快递公司创建独立专题页
→ **验证**：每个专题页可独立访问，查询入口可用

### Step 5：风控完善
1. 频次限制端到端测试
2. 次数用完后提示"明天再来"（V1 无广告）
→ **验证**：超过10次后提示限制

### Step 6：SEO 上线
1. sitemap.json 配置
2. 微信后台页面路径配置
3. 小程序名称和描述优化
→ **验证**：sitemap.json 配置正确，页面标题和路径配置完成（搜索收录为平台行为，不作硬性验证）

---

## 11. V2 预留

以下功能不在 V1 实现，但架构设计时需考虑扩展性：

- **广告变现**（Banner + 激励视频，需流量主资质后开启）
- 快递订阅推送（快递100 Subscription API + 微信订阅消息）
- 多包裹管理（同时跟踪多个快递）
- 云端历史同步（跨设备查询记录）
- 快递地图轨迹展示（快递100地图轨迹API）
- 快递时效预估
- 暗黑模式支持

---

## 12. 变更记录

### 2026-04-25 — Codex 评审修复
1. **分享链路**：从"好友可直接查看结果"改为"分享查询入口"，好友点击后跳转首页预填单号需自行查询
2. **频控规则**：消除"1分钟内查过拒绝"与"3次/分钟"的矛盾，统一为"两次查询间隔不少于20秒"（单个 lastQueryTime 字段即可实现）
3. **MD5 签名**：明确写入 `.toUpperCase()`，快递100要求32位大写十六进制
4. **顺丰手机号交互**：改为快递公司选择器方案（默认自动识别，手动选顺丰时显示手机号框）；自动识别模式下若后端报错需手机号，前端引导用户手动选择
5. **SEO 验收标准**：从"微信搜索可搜到"降级为"配置完成即通过"，搜索收录为平台行为不作硬性验证
6. **增长引擎定位**：标注 SEO 为非确定性结果，V1 仅做基础配置

### 2026-04-25 — Codex 第二轮评审修复
1. **reward 防刷**：移除无验证的 `bonus += 5`，改为 nonce 防重放 + 每日最多领3次奖励（上限25次/天）
2. **单号级缓存**：新增 `query_cache` 集合，同一单号30分钟内走缓存不调快递100 API，避免锁单
3. **移除 20 秒冷却期**：去掉用户级两次查询间隔限制，依赖每日上限 + 前端防抖 + 单号缓存即可
4. **错误码结构化**：新增 `PHONE_REQUIRED` 错误码，前端按 code 分支而非文案
5. **单号格式放宽**：允许字母、数字、连字符（-），兼容 EMS 国际件等特殊格式
6. **新增 reward_nonces 集合**：防止奖励 nonce 被重放使用
7. **rate_limit 新增 rewardCount 字段**：追踪每日广告奖励领取次数

### 2026-04-25 — 广告功能推迟到 V2
- 流量主资质暂未满足（需累计独立访客 >= 1000），广告变现功能整体推迟
- spec.md 第6章广告方案标注为"V2 实现"
- 实施计划 Step 5 从"广告与风控完善"改为"风控完善"
- V2 预留新增"广告变现"条目
- 首页 `_showLimitDialog` 改为仅提示"明天可继续使用"，移除 `_showRewardedAd` 方法
- user-stories.md 中 US-3.4/3.5/3.6 标注为 V2
